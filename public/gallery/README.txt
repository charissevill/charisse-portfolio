Put gallery photos here (e.g. 1.jpg, 2.jpg, ...), then set the
matching item's `src` in src/data/content.ts's `gallery` array, e.g.
src: "/gallery/1.jpg".

There are 8 placeholder tiles by default — fill in as many as you
have real photos for; the rest stay as empty placeholders.
