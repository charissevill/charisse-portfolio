Put your gallery images in this folder (e.g. 1.jpg, 2.jpg, ...),
then in src/data/content.ts set the `src` field on each gallery
item to match, e.g. "/gallery/1.jpg".

Until an item has a `src`, it renders as a placeholder tile.
